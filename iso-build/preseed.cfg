# Monolith FireWall Preseed Configuration
# For automated Debian installation

# Locale and keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network configuration (DHCP initially, managed by MonolithFireWall after install)
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string monolith-firewall
d-i netcfg/get_domain string local

# Mirror (use local if offline install)
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Root password (changed during setup wizard)
d-i passwd/root-password password monolith
d-i passwd/root-password-again password monolith

# No additional users (managed by MonolithFireWall)
d-i passwd/make-user boolean false

# Clock and timezone
d-i clock-setup/utc boolean true
d-i time/zone string UTC

# Partitioning (use entire disk, single partition)
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Base system installation
d-i base-installer/install-recommends boolean false
d-i base-installer/kernel/image string linux-image-amd64

# Package selection
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string monolith-firewall nftables iproute2 dnsmasq tcpdump vim-tiny tmux ifupdown2 sudo procps isc-dhcp-client iputils-ping
d-i pkgsel/upgrade select full-upgrade

# Boot loader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string /dev/sda

# Finish installation
d-i finish-install/reboot_in_progress note

# Late command: Install monolith packages and start setup wizard
d-i preseed/late_command string \
    in-target cp /media/cdrom/monolith-packages/*.mfwpkg /var/lib/monolith-firewall/packages/ 2>/dev/null || true; \
    in-target systemctl enable monolith-firewall-core.service; \
    in-target systemctl enable monolith-firewall-webui.service
